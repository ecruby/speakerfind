== Experts Dir Application setup

Clone the repository


== Set up configuration

Copy config/database.yml.example to config/database.yml.

  cp config/database.yml.example config/database.yml

==Run the setup task

  rake app:setup
